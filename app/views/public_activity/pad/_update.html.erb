<% if activity.trackable %>
  hat das Pad <%= link_to activity.trackable.title, activity.trackable %> aktualisiert.
<% else %>
  hat ein Pad, das in der Zwischenzeit gelöscht wurde, aktualisiert.
<% end %>