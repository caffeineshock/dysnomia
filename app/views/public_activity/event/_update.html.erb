<% if activity.trackable %>
  hat den Termin <%= link_to activity.trackable.title, activity.trackable %> aktualisiert.
<% else %>
  hat einen Termin, der in der Zwischenzeit gelöscht wurde, aktualisiert.
<% end %>