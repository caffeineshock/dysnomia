<% if activity.trackable %>
  hat die Seite <%= link_to activity.trackable.title, activity.trackable %> aktualisiert.
<% else %>
  hat eine Seite, die in der Zwischenzeit gelöscht wurde, aktualisiert.
<% end %>