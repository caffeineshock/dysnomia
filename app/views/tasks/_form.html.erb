<%= simple_form_for @task do |f| %>
  <%= f.error_notification %>
  <%= f.input :title %>
  <%= f.input :due_at, include_blank: true %>
  <%= f.input :completed %>
  <%= f.association :users, label_method: :username, collection: User.approved%>
  <%= f.button :submit %>
<% end %>